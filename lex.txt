sudo apt-get install flex

sudo apt-get install byacc

sudo apt-get install bison

sudo apt-get install bison++

sudo apt-get install byacc-j

---------------------------------------------

HOW TO COMPILE & RUN LEX

lex De

lex something.l

gcc lex.yy.c

./a.out

---------------------------------------------

HOW TO COMPILE & RUN YACC

lex something.l

yacc -d something.y

gcc lex.yy.c y.tab.c

./a.out

---------------------------------------------

FOR LEX FILE SAVE IT IN DESKTOP, GIVE ANYNAME WITH ".l" EXTENSION

THEN GO-TO TERMINAL

cd Desktop
ls(for all files)
flex xyz.l  (compile)
cc lex.yy.c -lfl(for flex)
./a.out(run)

-------------------------------------------------

ASSIGNMENT 8

#include<stdio.h>
#include<string.h>
struct op {
	char i[20];
	char r[20];
} op[10], pr[10];

void main() {
	int a,i,k,j,n,z=0,m,q,ln;
	char *p, *l;
	printf("\n Enter the number of values: ");
	scanf("%d", &n);
	for(i=0;i<n;i++){
		printf("\t left \t");
		scanf("%s", pr[i] n);
		printf("\t right \t");
		scanf("%s", op[i] n);
	}
	for(i=0;i<n;i++){
		printf("\n Line Number: %d \n", ln);
		printf("\t \t %s", op[i].n);
		printf("\t \t %s", op[i].n);
		ln++;
	}
	printf("\n Data flow analysis of the given code: \n");
	for(i=0;i<n;i++){
			for(j=0;j<n;j++){
			mtch = strstr(op[j].n, op[i].l);
			if(mtch){
				printf("An %s is live of %s \n", op[i].l, op[j].n);
			}
		}
	}
}